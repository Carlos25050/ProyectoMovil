/*package edu.unicauca.proyectomovil.data
import androidx.room.Entity
import androidx.room.PrimaryKey

@Entity(tableName = "foto_perfil")
data class FotoPerfil(
    @PrimaryKey val userId: Int = 1, // Usamos un ID único si solo hay un usuario
    val imageUri: String? // Almacena el URI de la imagen como String
)*/